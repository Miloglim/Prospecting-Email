# Prospecting Email — 公司背调 + 开发信生成工具

> 拖入客户表 → AI 背调 → 输出定制双语开发信 → 自动发送

---

## 一句话

把客户名单变成定制化的西语/葡语开发信，然后自动发出去。

---

## 能做什么

| 功能 | 怎么触发 | 说明 |
|------|---------|------|
| 🔍 公司背调 | 「分析客户表」 | 逐家搜索官网/新闻/进口特征，输出背调报告 |
| ✍️ 生成开发信 | 「生成开发信」 | 基于背调生成双语（西语+英语）开发信 |
| 📧 自动发送 | `node send/send.js <file>` | SMTP 发送，模拟人工延迟，工作时间窗口控制 |
| 🗂️ 通用模板 | 「使用通用模板」 | 15 套预置模板（3 类客户 × 5 跟进阶段），从预设句库组装 |
| 📊 飞书对接 | 给 Token 即可 | 直接读取飞书多维表格中的客户表 |

---

## 怎么用

### 1. 准备客户表

Excel/CSV，至少包含「公司名」列。额外的网站、国家、品类列能大幅提升背调精度。

### 2. 对 Claude 说

```
分析客户表          → AI 逐家背调，输出报告
生成开发信          → 基于背调生成双语开发信
用通用模板发直客    → 从 15 套模板中选直客序列，组装发送
```

### 3. 发送

```bash
node send/send.js send/send-batch.json
```

每天自动限额 500 封，仅在北京时间 19:00-03:00（拉美工作时间）发送，每封间隔 45-120 秒随机延迟。

---

## 文件结构

```
Prospecting Email/
├── README.md                     ← 你在这里
├── CLAUDE.md                     ← 项目指令
├── package.json                  ← npm 配置
│
├── docs/                         ← 参考文档
│   ├── company-data.md           ← YQN 公司事实库（写信用数字来源）
│   ├── email-writing-rules.md    ← 写作准则 + 拉美文化适配
│   └── email-standard.md         ← 邮件排版规范
│
├── templates/
│   └── general-templates.md      ← 通用模板库（15套双语 + 预设句库 + 垃圾词黑名单）
│
├── send/
│   ├── send.js                   ← 发送脚本（SMTP + 模拟延迟）
│   ├── extract.js                ← 从背调报告提取邮件队列
│   ├── import-csv.js             ← CSV → 邮件队列
│   ├── status.js                 ← 查看发送统计
│   ├── config.json               ← SMTP 配置 + 发送规则
│   ├── signature.html            ← HTML 签名档
│   ├── send-batch.json           ← 待发送邮件队列（运行时）
│   └── send-log.json             ← 发送日志（运行时）
│
├── reports/
│   └── 客户背调报告-*.md          ← AI 生成的背调报告
│
└── .claude/
    └── skills/
        └── company-researcher/   ← 公司背调技能定义
```

---

## 模板系统

### 三类客户

| 类型 | 特点 | 主要卖点 |
|------|------|---------|
| **代理** | 货代同行 | 资质（FIATA/AEO/NVOCC）+ 300+船司 + 舱位弹性 |
| **直客** | 进口商/终端 | Mercado Libre SPN + AEO + 墨西哥实体 + RFC 清关 |
| **未标签** | 身份未知 | FIATA + AEO + 35,000+企业背书 |

### 五个阶段

```
冷开发 → F1(7天) → F2(14天) → F3(21天) → F4(30天)
```

同一序列 5 封主题行完全一致，变的是内容语气（越来越轻、不催）。

### 使用方式

从预设句库选句 → 替换占位符 → 组装 → 发送。无需每次都从头写。

---

## 关键约束

- 🈲 代理不提本地仓库/本地团队
- 🈲 同一封不同时出现船东名 + 具体运价
- 🈲 不对海外客户用 digital/AI/平台等技术词汇
- 🈲 不用最高级/紧迫词/夸大承诺/价格诱饵（含垃圾词黑名单）
- ✅ 西语在前，英语在后
- ✅ 第二人称，不教客户做事
- ✅ CTA 是「给」不是「要」

---

## 依赖

- Node.js + nodemailer（发送）
- 飞书 CLI（`lark-cli`，读取多维表格）
- Tavily CLI（新闻搜索）
